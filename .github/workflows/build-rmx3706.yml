name: Build SukiSU Ultra for Realme GT Neo5 150W (RMX3706, SM8475, Android 16)

on:
  workflow_dispatch:
    inputs:
      KSU_CONFIG:
        description: "SukiSU 分支"
        required: true
        type: choice
        options:
          - SukiSU_dev
          - SukiSU_stable
        default: SukiSU_dev
      SUSFS:
        description: "启用 SUSFS"
        required: true
        type: choice
        options:
          - enable
          - disable
        default: enable
      VFS:
        description: "启用 VFS 补丁"
        required: true
        type: choice
        options:
          - enable
          - disable
        default: enable
      KPM:
        description: "启用 KPM"
        required: true
        type: choice
        options:
          - enable
          - disable
        default: enable
      LZ4_ZSTD:
        description: "启用 lz4/zstd 优化"
        required: true
        type: choice
        options:
          - enable
          - disable
        default: enable

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 8192
          temp-reserve-mb: 2048
          swap-size-mb: 8192
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'

      - name: Install dependencies (Azure mirror)
        run: |
          git config --global user.name "build-bot"
          git config --global user.email "bot@localhost"
          sudo sed -i 's|http://archive.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list.d/ubuntu.sources 2>/dev/null || true
          sudo sed -i 's|http://security.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list.d/ubuntu.sources 2>/dev/null || true
          sudo sed -i 's|http://archive.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list 2>/dev/null || true
          sudo sed -i 's|http://security.ubuntu.com/ubuntu|http://azure.archive.ubuntu.com/ubuntu|g' /etc/apt/sources.list 2>/dev/null || true
          sudo apt update -o Acquire::Retries=5 -o Acquire::http::Timeout=30 -y
          sudo DEBIAN_FRONTEND=noninteractive apt install -y --no-install-recommends \
            -o Acquire::Retries=5 -o Acquire::http::Timeout=30 \
            python3 git curl wget unzip zip xz-utils zstd bc bison flex \
            libssl-dev libelf-dev libncurses-dev make ccache dwarves pahole rsync

      - name: Download prebuilt clang r416183b
        run: |
          mkdir -p kernel_workspace/prebuilts-master/clang/host/linux-x86
          cd kernel_workspace/prebuilts-master/clang/host/linux-x86
          curl -L --retry 5 --retry-delay 5 -o clang-r416183b.tar.gz \
            "https://gitlab.com/CalyxOS/platform_prebuilts_clang_kernel_linux-x86_clang-r416183b/-/archive/android14/platform_prebuilts_clang_kernel_linux-x86_clang-r416183b-android14.tar.gz"
          mkdir -p clang-r416183b
          tar -xzf clang-r416183b.tar.gz -C clang-r416183b --strip-components=1 || tar -xzf clang-r416183b.tar.gz -C clang-r416183b
          rm -f clang-r416183b.tar.gz
          clang-r416183b/bin/clang --version | head -1

      - name: Pull kernel source + patches
        run: |
          mkdir -p kernel_workspace/kernel_platform
          cd kernel_workspace/kernel_platform
          # OPPO SM8475 官方开源内核 (Android 16 / 5.10.236)
          git clone --depth=1 https://github.com/oppo-source/android_kernel_common_oppo_sm8475 \
            -b oppo/sm8475_b_16.0_reno9_pro_plus common
          git clone --depth=1 https://github.com/oppo-source/android_kernel_oppo_sm8475 \
            -b oppo/sm8475_b_16.0_reno9_pro_plus msm-kernel
          # SukiSU 补丁仓库
          git clone --depth=1 https://github.com/SukiSU-Ultra/SukiSU_patch.git
          # SUSFS 补丁仓库
          git clone --depth=1 -b gki-android12-5.10 https://github.com/ShirkNeko/susfs4ksu.git
          # 去除 -dirty 后缀
          sed -i 's/ -dirty//g' common/scripts/setlocalversion || true
          sed -i '$i res=$(echo "$res" | sed '\''s/-dirty//g'\'')' common/scripts/setlocalversion || true

      - name: Convert OGKI to GKI (hmbird patch)
        run: |
          cd kernel_workspace/kernel_platform/common
          # OPPO 魔改 GKI(OGKI) 转标准 GKI, 移除对未开源 oplus 私有代码的依赖
          sed -i '1iobj-y += hmbird_patch.o' drivers/Makefile
          curl -L --retry 5 --retry-delay 3 -o hmbird_patch.patch \
            "https://raw.githubusercontent.com/TNTyep520/Actions_SukiSU_oppo_kernel_sm8475/SukiSU-Ultra/Patches/hmbird_patch.patch"
          ls -la hmbird_patch.patch
          patch -p1 -F 3 < hmbird_patch.patch || echo "hmbird patch may partially apply, continuing..."

      - name: Auto-fix missing Kconfig references
        run: |
          cd kernel_workspace/kernel_platform/common
          # 扫描所有 Kconfig 的 source 引用, 为缺失路径自动创建占位
          # 解决 OPPO 开源不完整导致的 "can't open file" 报错
          grep -rhoE '^[[:space:]]*source[[:space:]]+"[^"]+"' --include="Kconfig" . 2>/dev/null \
            | sed -E 's/^[[:space:]]*source[[:space:]]+"//; s/"$//' \
            | sort -u > /tmp/kconfig_sources.txt
          echo "Total Kconfig source refs: $(wc -l < /tmp/kconfig_sources.txt)"
          while IFS= read -r path; do
            [ -z "$path" ] && continue
            # 跳过含变量的路径
            case "$path" in *'$'*) continue ;; esac
            # 目录不存在则创建; 文件不存在则创建占位 (touch 不报错)
            mkdir -p "$(dirname "$path")" 2>/dev/null || true
            if [ ! -f "$path" ]; then
              echo "# placeholder for missing (OPLUS private) Kconfig" > "$path"
              echo "Created placeholder: $path"
            fi
          done < /tmp/kconfig_sources.txt
          # 额外处理: 某些 Kconfig 通过 obj-y 引用 .c 文件而不是 source, 这些目录存在但内容缺失
          # 再次扫描 Makefile 中 oplus 相关 obj 引用并确保对应 .c 文件存在占位
          echo "Placeholder fix done."

      - name: Add SukiSU Ultra
        run: |
          cd kernel_workspace/kernel_platform/common
          if [ "${{ inputs.KSU_CONFIG }}" = "SukiSU_stable" ]; then
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-stable
          else
            curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setup.sh" | bash -s susfs-dev
          fi
          cd ./KernelSU
          KSU_VERSION=$(expr $(/usr/bin/git rev-list --count main) "+" 10606)
          echo "KSUVER=$KSU_VERSION" >> $GITHUB_ENV
          export KSU_VERSION=$KSU_VERSION
          sed -i "s/DKSU_VERSION=12800/DKSU_VERSION=${KSU_VERSION}/" kernel/Makefile

      - name: Apply SUSFS patches
        if: ${{ inputs.SUSFS == 'enable' }}
        run: |
          cd kernel_workspace/kernel_platform
          cp ../susfs4ksu/kernel_patches/50_add_susfs_in_gki-android12-5.10.patch ./common/
          cp ../susfs4ksu/kernel_patches/fs/* ./common/fs/ 2>/dev/null || true
          cp ../susfs4ksu/kernel_patches/include/linux/* ./common/include/linux/ 2>/dev/null || true
          cd ./common
          patch -p1 -F 3 < 50_add_susfs_in_gki-android12-5.10.patch || echo "susfs patch may partially apply"
          cp ../../SukiSU_patch/69_hide_stuff.patch ./
          patch -p1 -F 3 < 69_hide_stuff.patch || echo "hide_stuff may partially apply"

      - name: Apply VFS patches
        if: ${{ inputs.VFS == 'enable' }}
        run: |
          cd kernel_workspace/kernel_platform/common
          cp ../../SukiSU_patch/hooks/syscall_hooks.patch ./
          patch -p1 -F 3 < syscall_hooks.patch || echo "vfs may partially apply"

      - name: Apply lz4/zstd patches
        if: ${{ inputs.LZ4_ZSTD == 'enable' }}
        run: |
          cd kernel_workspace/kernel_platform/common
          cp -r ../../SukiSU_patch/other/zram/lz4k/include/linux/* ./include/linux/ 2>/dev/null || true
          cp -r ../../SukiSU_patch/other/zram/lz4k/lib/* ./lib/ 2>/dev/null || true
          cp -r ../../SukiSU_patch/other/zram/lz4k/crypto/* ./crypto/ 2>/dev/null || true
          cp -r ../../SukiSU_patch/other/zram/lz4k_oplus ./lib/ 2>/dev/null || true

      - name: Kernel configuration
        run: |
          cd kernel_workspace/kernel_platform
          CONFIG_FILE=./common/arch/arm64/configs/gki_defconfig
          echo "CONFIG_KSU=y" >> "$CONFIG_FILE"
          if [ "${{ inputs.KPM }}" = "enable" ]; then
            echo "CONFIG_KPM=y" >> "$CONFIG_FILE"
          fi
          if [ "${{ inputs.VFS }}" = "enable" ]; then
            echo "CONFIG_KSU_MANUAL_HOOK=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_WITH_KPROBES=n" >> "$CONFIG_FILE"
          fi
          if [ "${{ inputs.SUSFS }}" = "enable" ]; then
            echo "CONFIG_KSU_SUSFS=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_PATH=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_KSTAT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SUS_OVERLAYFS=n" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_TRY_UMOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SPOOF_UNAME=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_ENABLE_LOG=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y" >> "$CONFIG_FILE"
            echo "CONFIG_KSU_SUSFS_OPEN_REDIRECT=y" >> "$CONFIG_FILE"
          fi
          if [ "${{ inputs.LZ4_ZSTD }}" = "enable" ]; then
            echo "CONFIG_CRYPTO_LZ4=y" >> "$CONFIG_FILE"
            echo "CONFIG_CRYPTO_LZ4HC=y" >> "$CONFIG_FILE"
            echo "CONFIG_CRYPTO_LZ4K=y" >> "$CONFIG_FILE"
            echo "CONFIG_CRYPTO_LZ4KD=y" >> "$CONFIG_FILE"
            echo "CONFIG_CRYPTO_842=y" >> "$CONFIG_FILE"
            echo "CONFIG_ZSMALLOC=y" >> "$CONFIG_FILE"
            echo "CONFIG_TMPFS_XATTR=y" >> "$CONFIG_FILE"
            echo "CONFIG_TMPFS_POSIX_ACL=y" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_ADVANCED=y" >> "$CONFIG_FILE"
            echo "CONFIG_TCP_CONG_BBR=y" >> "$CONFIG_FILE"
            echo "CONFIG_NET_SCH_FQ=y" >> "$CONFIG_FILE"
          fi
          sed -i 's/check_defconfig//' ./common/build.config.gki || true

      - name: Build Kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/kernel_workspace/prebuilts-master/clang/host/linux-x86/clang-r416183b/bin:$PATH"
          export PATH="/usr/lib/ccache:$PATH"
          export CC="ccache clang"
          cd kernel_workspace/kernel_platform/common
          MAKE_ARGS="LLVM=1 LLVM_IAS=1 ARCH=arm64 \
            CROSS_COMPILE=aarch64-linux-gnu- \
            LD=ld.lld HOSTLD=ld.lld KCFLAGS+=-Wno-error"
          make -j$(nproc --all) O=out $MAKE_ARGS gki_defconfig 2>&1 | tail -60
          make -j$(nproc --all) O=out $MAKE_ARGS 2>&1 | tail -80
          ccache -s
          find out -name "Image" -o -name "Image.gz" | head -10

      - name: Package AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3 --depth=1
          rm -rf ./AnyKernel3/.git ./AnyKernel3/README.md
          sed -i "s|BLOCK=/dev/block/platform/omap/omap_hsmmc.0/by-name/boot;|BLOCK=auto;|" AnyKernel3/anykernel.sh
          sed -i "s/IS_SLOT_DEVICE=0/IS_SLOT_DEVICE=auto/" AnyKernel3/anykernel.sh
          sed -i "s/do.devicecheck=1/do.devicecheck=0/" AnyKernel3/anykernel.sh
          image_path=$(find "$GITHUB_WORKSPACE/kernel_workspace/kernel_platform/common/out/" -name "Image" | head -n 1)
          if [ -n "$image_path" ] && [ -f "$image_path" ]; then
            echo "Image located: $image_path"
            cp "$image_path" ./AnyKernel3/Image
          else
            echo "Image not found! Build failed" >&2
            exit 1
          fi

      - name: Upload AnyKernel3
        uses: actions/upload-artifact@v4
        with:
          name: RMX3706_SukiSUUltra_${{ env.KSUVER }}_AnyKernel3
          path: ./AnyKernel3/*
